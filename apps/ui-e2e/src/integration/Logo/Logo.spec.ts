describe('ui: Logo component', () => {
  beforeEach(() => cy.visit('/iframe.html?id=layout-header--logo'))

  it('should render the component', () => {
    // cy.get('h1').should('contain', 'Welcome to ui!')
  })
})
